/*
movie.ts defiend Movie class
*/

export class Movie {
    id?: number;
    name?: string;
    releaseYear?: number;

    // constructor(x: number, y: string, z: number) {
    //     this.id = x;
    //     this.name = y;
    //     this.releaseYear = z;
    // }
}
